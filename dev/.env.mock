# Passport API Configuration for Mock Development
# These are dummy values used when running with MSW mocks
VITE_API_KEY=test-api-key
VITE_SCORER_ID=123

# Mock Service Worker (MSW)
# Intercepts API calls with mock data
VITE_ENABLE_MSW=true

# Optional: Override the embed service URL
# VITE_EMBED_SERVICE_URL=http://localhost:8004